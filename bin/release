#!/bin/bash

ORIG_FILE=src/BowlingBall.tsx
DIST=dist

mkdir -p $DIST
cp $ORIG_FILE $DIST
cp $ORIG_FILE $DIST/BowlingBallCompat.tsx

# rm $COMPAT_FILE
# cp $ORIG_FILE $COMPAT_FILE

sed -i '1s/^/import { createElement } from "react";\n/' $DIST/BowlingBallCompat.tsx

